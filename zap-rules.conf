# ZAP Baseline Scan — Rule Configuration
# These WARN rules are expected for a Next.js application and are not
# real security vulnerabilities. They are suppressed to prevent false
# CI failures while still catching any unexpected new alerts.
#
# Format: <rule-id> <action> (<description>)
# Actions: IGNORE, WARN, FAIL

# Next.js bundled JS contains build comments — not a real disclosure risk
10027	IGNORE	(Information Disclosure - Suspicious Comments)

# Next.js framework internals use eval-like patterns — expected behavior
10110	IGNORE	(Dangerous JS Functions)

# Next.js sets session-related headers by default
10112	IGNORE	(Session Management Response Identified)

# SPA/SSR frameworks are always flagged as "modern web application"
10109	IGNORE	(Modern Web Application)

# Next.js redirect responses don't set cache-control — not exploitable
10015	IGNORE	(Re-examine Cache-control Directives)
